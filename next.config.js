/** @type {import('next').NextConfig} */
const nextConfig = {
  // Enables the use of Turbopack for faster local development, 
  // which was indicated in your local setup.
  // This line is optional but recommended if you are using Turbopack.
  // experimental: {
  //   serverComponentsExternalPackages: ["@google/genai"],
  // },

  // Add configuration for images or other standard Next.js features here
  // For example:
  // images: {
  //   domains: ['cdn.example.com'],
  // },
};

module.exports = nextConfig;